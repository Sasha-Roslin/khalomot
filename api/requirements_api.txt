# Keter API Requirements
# =======================
# Consolidated dependencies for API + Streamlit + RL Training
# Uses CPU-only PyTorch (no GPU on Cloud Run)

# Core scientific
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Optimization
scikit-optimize>=0.9.0

# API framework
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
pydantic>=1.8.0
aiofiles>=0.8.0
python-multipart>=0.0.5
python-dotenv>=1.0.0

# Config & serialization
PyYAML>=6.0

# Cloud storage
google-cloud-storage>=2.0.0,<3.0.0
protobuf>=4.21.0,<5.0.0

# HTTP client (for streamlit -> API)
requests>=2.26.0
httpx>=0.24.0

# Visualization
plotly>=5.0.0
matplotlib>=3.3.0

# Graph / DAG / Causal
networkx>=2.6.0
causalnex>=0.11.0
tensorflow-cpu>=2.10.0,<2.21.0

# Streamlit frontend
streamlit>=1.20.0

# RL Agent (Tectonic Classification)
# CPU-only PyTorch installed separately (see Dockerfile) to avoid index conflicts
torch>=2.0.0
stable-baselines3>=2.0.0
gymnasium>=0.28.0

# Geospatial
geopy>=2.3.0

# Excel file support
openpyxl>=3.0.0

# LLM Agent
google-generativeai>=0.8.0
google-genai>=0.1.1

